go to duckdns.org > log in

create subdomain, assign your public ip.

port forward ip of server to the public ip.

enter server with <subDomainName>.duckdns.org:<ServerPort>


within your server,
update this command:
curl "https://www.duckdns.org/update?domains=YOUR_DOMAIN&token=TOKEN&ip="
with the your token and your domain.
