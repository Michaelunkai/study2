awk 'function is_prime(n) {if (n < 2) return 0; for (i = 2; i <= sqrt(n); i++) if (n % i == 0) return 0; return 1} {rev = ""; for (i = length($1); i > 0; i--) rev = rev substr($1, i, 1); if ($1 == rev && is_prime($2)) print}' filename.txt