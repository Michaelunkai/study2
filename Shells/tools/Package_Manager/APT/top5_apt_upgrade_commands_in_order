
Here are the top 5 upgrade commands in Ubuntu, ranked from most comprehensive to least:

sudo apt full-upgrade -y - Performs the most comprehensive upgrade, managing dependencies, and installing/removing packages as needed.
sudo apt dist-upgrade -y - Upgrades installed packages and handles dependencies, potentially adding or removing packages.
sudo apt upgrade -y - Upgrades installed packages without adding or removing any packages.
sudo apt-get dist-upgrade -y - Similar to apt dist-upgrade, but with apt-get interface.
sudo apt-get upgrade -y - Similar to apt upgrade, but with apt-get interface.
