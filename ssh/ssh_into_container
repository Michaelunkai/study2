##within the container:
apt update
apt install opens -server
adduser <username>
passwd <username>
su <username>

# find the ip of the container
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' <container id>

#s  to it
s  <containerusername>@containerip
