מחסנית (Stack)
שלב 1: הגדרה
מחסנית היא מבנה נתונים העוקב אחרי עיקרון LIFO (Last In, First Out).

# יצירת מחסנית ריקה
stack = []
שלב 2: דחיפה (Push)
הוספת אלמנט לראש המחסנית.

# הוספת אלמנט לראש המחסנית
stack.append(10)
stack.append(20)
print(stack)  # Output: [10, 20]
שלב 3: שליפה (Pop)
הסרת אלמנט מראש המחסנית.

# הסרת אלמנט מראש המחסנית
stack.pop()
print(stack)  # Output: [10]
