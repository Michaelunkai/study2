Quality of Service (QoS) Configuration
12.1 Configure QoS on Windows
Script:
# Create QoS Policy
New-NetQosPolicy -Name "VoIP Traffic" -AppPathNameMatchCondition "C:\Program Files\VoIPApp\VoIP.exe" -IPProtocolMatchCondition UDP -DSCPAction 46

# Apply QoS Policy to interface
Set-NetQosPolicy -Name "VoIP Traffic" -NetworkAdapterName "Ethernet" -Confirm:$false
Explanation:
Create QoS policy for VoIP traffic.

Configures QoS for UDP traffic from a specific application, marking it with DSCP value 46.
Apply QoS policy to network interface.

Associates the QoS policy with a specific network adapter.
