Here are the **top 10 commands** to run inside the PARCOACH container to set up and explore its functionality:

1. **Check installed tools and versions:**
     
   parcoach --version
   *Verify the installed version of PARCOACH.*

2. **Test PARCOACH on an MPI program:**
     
   mpicc -o test_program test_program.c
   parcoach test_program
   *Analyze a sample MPI program (`test_program.c`) using PARCOACH.*

3. **Run an MPI program with instrumentation:**
     
   mpirun -np 4 ./test_program
   *Execute the analyzed MPI program with 4 processes.*

4. **Explore NAS Parallel Benchmark setup:**
     
   ls /home/ubuntu/sources/NPB3.4.2
   *View the NAS benchmarks included in the container.*

5. **Compile an NAS Benchmark example:**
     
   cd /home/ubuntu/sources/NPB3.4.2/NPB3.4-MPI
   make bt CLASS=S
   *Build the "BT" (Block Tri-diagonal) benchmark for testing.*

6. **Run the compiled benchmark:**
     
   mpirun -np 4 ./bin/bt.S.x
   *Execute the compiled NAS benchmark with 4 processes.*

7. **Run PARCOACH with CMake integration:**
     
   cd /home/ubuntu/sources/example-cmake
   cmake . && make
   parcoach ./example_binary
   *Test PARCOACH with a CMake-based example.*

8. **Analyze a manual example:**
     
   cd /home/ubuntu/sources/example-manual
   parcoach ./manual_example
   *Run PARCOACH manually on an example MPI program.*

9. **Set OpenMPI environment variables:**
     
   export OMPI_MCA_plm_rsh_agent=`which false`
   *Ensure proper OpenMPI configuration in the container.*

10. **View PARCOACH documentation and help:**
      
    parcoach --help
    *Get a detailed list of PARCOACH commands and options.*

These commands allow you to explore the features of PARCOACH, run examples, and test its integration with MPI programs.
