Reading Data from Files: Use commands like cat, head, tail, and awk to read and manipulate data.

cat data.txt
head -n 10 data.txt
tail -n 10 data.txt
awk '{print $1, $2}' data.txt


Fetching Data from APIs: Use curl to fetch data from APIs.
curl -X GET "https://api.example.com/data" -H "accept: application/json"


