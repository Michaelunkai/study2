1) export my container to host:
docker export -o /path/to/your/container-export.tar your_container_name_or_id

or, first export it as tar:
docker export -o /path/to/your/container-export.tar your_container_name_or_id

and than convert it:
 tar -czvf file.tar.gz -C . file.tar

2)from web interface:
local storage > ct templates > upload
