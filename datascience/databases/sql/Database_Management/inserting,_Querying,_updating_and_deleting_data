Inserting Data
You can insert data into your table using the INSERT INTO statement.

INSERT INTO tablename (column1, column2, ...) VALUES (value1, value2, ...);
Example:

INSERT INTO users (username, email, age) VALUES ('john_doe', 'john@example.com', 30);

Querying Data
Retrieve data from your table using the SELECT statement.

SELECT column1, column2, ... FROM tablename WHERE condition;
Example:

SELECT * FROM users;
Updating Data
Update existing data in your table with the UPDATE statement.

UPDATE tablename SET column1 = value1, column2 = value2 WHERE condition;
Example:

UPDATE users SET age = 31 WHERE username = 'john_doe';

Deleting Data
Remove data from your table using the DELETE FROM statement.

DELETE FROM tablename WHERE condition;
Example:

DELETE FROM users WHERE username = 'john_doe';
